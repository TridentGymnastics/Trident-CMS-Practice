---
import notice from '../content/site-notice.json';
---

{notice.enabled && (
  <aside class={`site-notice site-notice--${notice.tone}`} aria-label="Club notice" data-site-notice>
    <div class="container">
      <strong>{notice.heading}</strong>
      {notice.message && <span>{notice.message}</span>}
      {notice.link_text && notice.link_href && <a href={notice.link_href}>{notice.link_text}</a>}
    </div>
  </aside>
)}

<style>
  .site-notice { background: #eaf4fc; color: #151957; padding: 1rem 0; border-bottom: 1px solid #c7def2; }
  .site-notice--important { background: #ffe281; border-color: #e5c35b; }
  .container { display: flex; flex-wrap: wrap; justify-content: center; align-items: baseline; gap: .4rem 1rem; text-align: center; }
  strong { font-weight: 700; }
  span { white-space: pre-line; }
  a { color: inherit; text-decoration: underline; font-weight: 700; }
.site-notice--success { background: #dcfce7; color: #14532d; border-color: #14532d; }
.site-notice--urgent { background: #ffe4e6; color: #881337; border-color: #881337; }
.site-notice--lavender { background: #ede9fe; color: #4c1d95; border-color: #4c1d95; }
.site-notice--peach { background: #ffedd5; color: #7c2d12; border-color: #7c2d12; }
.site-notice--navy { background: #151957; color: #ffffff; border-color: #ffffff; }
.site-notice--teal { background: #ccfbf1; color: #134e4a; border-color: #134e4a; }
</style>
